[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "news-bulletin-playlist"
version = "0.0.1"
description = "Open-source service for dynamic news bulletin playlists across countries and languages"
readme = "README.md"
requires-python = ">=3.14"
license = "MIT"
authors = [{ name = "news-bulletin-playlist contributors" }]
dependencies = ["PyYAML>=6.0.3,<7"]

[project.scripts]
news-playlist = "news_bulletin_playlist.cli:main"

[project.optional-dependencies]
dev = [
  "pytest>=8.3,<10",
  "ruff>=0.12,<1",
  "mypy>=2.3,<3",
]

[tool.hatch.build.targets.wheel]
packages = ["src/news_bulletin_playlist"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.14"
strict = true
packages = ["news_bulletin_playlist"]
mypy_path = "src"
